import React, {useEffect} from 'react';

const StarRating = ({vote_avenger}) => {
    useEffect(() => {

    }, [vote_avenger])
    console.log(vote_avenger);
    return (
        <div>

        </div>
    );
};

export default StarRating;